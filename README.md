## wedding (Vite)

Standalone Vite + React + TS + Tailwind v4 + shadcn/ui version.

### Run

```bash
pnpm install
pnpm dev
```

### Naver Map env

Create `.env.local`:

```bash
VITE_NAVER_MAP_CLIENT_ID=YOUR_KEY
```

You can use `env.sample` as a template (rename/copy it to `.env.local`).

